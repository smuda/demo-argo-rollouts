apiVersion: v1
kind: Service
metadata:
  name: cider-stable
  labels:
    app: cider
    role: stable
spec:
  selector:
    app: cider
  ports:
    - name: http
      port: 8080
      targetPort: 8080
