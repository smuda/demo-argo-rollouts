apiVersion: v1
kind: Service
metadata:
  name: cider-canary
  labels:
    app: cider
    role: canary
spec:
  selector:
    app: cider
  ports:
    - name: http
      port: 8080
      targetPort: 8080
